# Shared onboarding workbook callouts repair

Replace these two files in the repository:

- `js/app.js`
- `js/content-sprint-13.js`

This repair:

- adds the new workbook callout sections to the page navigation so they render;
- restores the complete `marketplacer-services` page content;
- preserves the existing Seller Playbook additions in Sprint 13;
- keeps the workbook template link labelled as a preview only.

Suggested branch:

`fix/resources-workbook-callouts-services`

Suggested commit:

`Fix workbook callouts and restore Marketplacer Services`
